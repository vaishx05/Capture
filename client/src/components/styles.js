import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
    ul: {
        margin : '2rem',
        justifyContent: 'center',
        padding: '10px',
        borderRadius: '15px',
    }
}))